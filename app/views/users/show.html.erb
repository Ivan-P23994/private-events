
<%= @user.name %>

<% @organized_events.each do |event|%>
  <%= event.title %>
  <%= event.location %>
<% end %>

<p>upcoming events</p><br>
<% @future_events.each do |event|%>
  
  <%= event.title if event.attendees.find_by(id: current_user.id)%><br>
  
<% end %>
  <br>
<p>past events</p><br>
<% @past_events.each do |event|%>
  
   <%= event.title if event.attendees.find_by(id: current_user.id)%><br>

<% end %>


<%= link_to "Home", root_path %>
