<%= @chosen_event.title %>
<%= @chosen_event.location %>

<%= @chosen_event.attendees.each do |guest|%>
<br>
  <br>
  <%= guest.name%>
  <br>
  <br>
<% end %>
 <br>
  <br>


<%= link_to "Join Event", new_invitation_path(:event_id => @chosen_event.id)%>

<%= link_to "Home", root_path %>